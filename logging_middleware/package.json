{
  "name": "logging_middleware",
  "version": "1.0.0",
  "description": "Reusable logging middleware that sends logs to the evaluation test server",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "axios": "1.6.8"
  },
  "devDependencies": {
    "typescript": "5.4.5",
    "@types/node": "20.12.7"
  }
}
